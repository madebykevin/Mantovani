"""
Entrega 4 (Parte 1) — Geração de Código Intermediário (Três Endereços - TAC).

Converte a AST anotada em uma sequência linear de quádruplas/instruções TAC,
assegurando que cada instrução aritmética/lógica contenha no máximo um operador.
"""
from typing import List, Any
from mplc.erros import NaoImplementado


def gerar(arvore_ast: Any, tabela_simbolos: Any) -> Any:
    """
    Produz a representação intermediária linearizada a partir da AST e da tabela de símbolos.
    """
    raise NaoImplementado('o codigo de tres enderecos (Entrega 4)')


def despejar(codigo_tac: Any) -> List[str]:
    """
    Retorna as linhas formatadas do código intermediário (--ir) para inspeção.
    """
    raise NaoImplementado('o despejo do codigo intermediario (Entrega 4)')
